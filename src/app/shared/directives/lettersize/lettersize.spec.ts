import { Lettersize } from './lettersize';

describe('Lettersize', () => {
  it('should create an instance', () => {
    const directive = new Lettersize();
    expect(directive).toBeTruthy();
  });
});
